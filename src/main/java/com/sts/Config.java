package com.sts;

import org.springframework.boot.SpringApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class Config {

	/*
	@Bean
	public Person person()
	{
		
		return new Person(); // this person class ka jo object retrn hoga as a bean register hoga container ke andar
	
		return new Person(food()); // remove autowired anootation
	}
	
	@Bean
	public Food food() 
	{
		return new Food();
	}
	
	*/
}
